package icy.betterhorses.net;

/**
 * Legacy placeholder kept so any stale references still compile. Hitchpost placement no longer
 * uses a pending tracker or vanilla leash flow.
 */
@Deprecated(forRemoval = false)
public final class HitchpostPlacementTracker {
    private HitchpostPlacementTracker() {}
}
