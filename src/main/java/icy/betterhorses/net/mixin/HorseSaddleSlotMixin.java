package icy.betterhorses.net.mixin;

// Dormant 1.21.10 placeholder: saddle insertion now flows through the equipment-slot system, so the old HorseInventoryMenu$1.mayPlace target no longer exists. Kept for future migration but not annotated as a mixin.
public final class HorseSaddleSlotMixin {

    private HorseSaddleSlotMixin() {
    }
}
